#pragma once
#include <Arduino.h>

namespace APISrv
{
  /*
  const uint32_t CONFIG_PAGE_SIZE = 1716;  // per Hand vom Script nachtragen
  const uint8_t CONFIG_PAGE_CONTENT[] PROGMEM = { XXXX };
  */
#include "config.hex"
#include "config.inc"
}  // namespace APISrv